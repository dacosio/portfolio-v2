import React from "react";
import { InfiniteSlideProps } from "./InfiniteSlide.props";
import { Container } from "./InfiniteSlide.style";
import Marquee from "react-fast-marquee";

const InfiniteSlide = (props: InfiniteSlideProps): JSX.Element => {
  return <Marquee> ..html.css/react.___ </Marquee>;
};

export default React.memo(InfiniteSlide);
