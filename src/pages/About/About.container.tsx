import React from 'react';

import AboutView from './About.view';

const About = (): JSX.Element => {
  const generatedProps = {
    // generated props here
  };
  return <AboutView {...generatedProps} />;
};

export default About;
