import { AboutGeneratedProps } from "./About.props";
import { Container } from "./About.style";

const AboutView = (props: AboutGeneratedProps) => {
  return <Container>ABOUT</Container>;
};

export default AboutView;
